---
- hosts: all
  vars:
    ansible_python_interpreter: auto
  roles:
    - {
        role: "role_under_test",
        firewall_used: docker,
        docker_test: true,
        option_install_epel: false
      }
